import type { RequestHandler } from "express";

/**
 * @controller POST /api/forgotPassword
 * @description Forgot password
 * @access Public
 */
export const forgotPassword: RequestHandler = async (req, res) => {};
