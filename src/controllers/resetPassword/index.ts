import type { RequestHandler } from "express";

/**
 * @controller POST /api/resetPassword
 * @description Reset password
 * @access Private
 */
export const resetPassword: RequestHandler = async (req, res) => {};
